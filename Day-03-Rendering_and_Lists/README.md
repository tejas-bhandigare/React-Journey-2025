📘 Day 3 — Rendering & Lists
🎯 Goals

Learn conditional rendering (if, ternary ? :, &&).

Learn lists rendering with .map().

Understand keys in lists.

Build:
✅ Todo List App (add/remove tasks dynamically).
